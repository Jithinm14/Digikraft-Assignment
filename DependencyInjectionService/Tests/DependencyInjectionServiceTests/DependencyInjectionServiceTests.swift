import XCTest
@testable import DependencyInjectionService

final class DependencyInjectionServiceTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(DependencyInjectionService().text, "Hello, World!")
    }
}
